package org.example.manager;

import org.example.entity.Gate;

public class GateManager {
    Gate gate;

    public GateManager(Gate gate) {
        this.gate = gate;
    }

    public boolean parkVehicle
}
